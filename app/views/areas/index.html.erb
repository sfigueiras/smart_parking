<div class="row">
  <div class="col s12 offset-l2 l8">
    <h4>Areas</h4>
    <ul class="collection with-header" data-collapsible="accordion">
    
    <p class="flow-text">Debe poseer seleccionada una patente para realizar una Reserva. Podr&aacute;
      realizar mas de una reserva &uacute;nicamente con patentes diferentes</p>

    <% @areas.each do |area| %>
      <%= render partial: "area", locals:{area: area} %>
    <% end %>

  </div>
</div>
